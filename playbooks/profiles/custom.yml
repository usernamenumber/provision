---
### AUTO-GENERATED (changes will be lost) ###
- hosts: custom
  roles:
    - { role: apt_basic, when: apt_basic__enabled is defined and apt_basic__enabled }
    - { role: apt_localrepo, when: apt_localrepo__enabled is defined and apt_localrepo__enabled }
    - { role: bind, when: bind__enabled is defined and bind__enabled }
    - { role: build_iso, when: build_iso__enabled is defined and build_iso__enabled }
    - { role: debops.ifupdown, when: debops.ifupdown__enabled is defined and debops.ifupdown__enabled }
    - { role: desktop, when: desktop__enabled is defined and desktop__enabled }
    - { role: dhcpd, when: dhcpd__enabled is defined and dhcpd__enabled }
    - { role: edx, when: edx__enabled is defined and edx__enabled }
    - { role: external_data_drive, when: external_data_drive__enabled is defined and external_data_drive__enabled }
    - { role: iptables, when: iptables__enabled is defined and iptables__enabled }
    - { role: kalite, when: kalite__enabled is defined and kalite__enabled }
    - { role: php, when: php__enabled is defined and php__enabled }
    - { role: portal, when: portal__enabled is defined and portal__enabled }
    - { role: test_portal, when: test_portal__enabled is defined and test_portal__enabled }
    - { role: test_site, when: test_site__enabled is defined and test_site__enabled }
    - { role: users, when: users__enabled is defined and users__enabled }
    - { role: vidtest, when: vidtest__enabled is defined and vidtest__enabled }
    - { role: wap, when: wap__enabled is defined and wap__enabled }
    - { role: wikipedia, when: wikipedia__enabled is defined and wikipedia__enabled }
