location {{ item.urlpath }} {
	{% if item.port is defined %}
		rewrite {{ item.urlpath }}$ {{ item.urlpath }}/;
		rewrite {{ item.urlpath }}/(.*)$ http://{{ provision__lan_iface.ipv4.address }}:{{ item.port }}/$1;
	{% else %}
		alias {{ item.docroot }};
	{% endif %}
}
